# Optional environment variables for future authenticated checks.
# Public catalog generation must not require these values.

OPENROUTER_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional metadata sent to OpenRouter for polite API access.
HTTP_REFERER=
X_TITLE=free-llm-api-watch
